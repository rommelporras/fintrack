# Secret values are managed via environment-specific overlays or external secret managers.
# This template shows required keys — replace placeholder values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: fintrack-secrets
  namespace: fintrack
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://finance:changeme@postgres:5432/finance_db"
  REDIS_URL: "redis://redis:6379/0"
  JWT_SECRET_KEY: "replace-with-openssl-rand-hex-32"
  POSTGRES_USER: "finance"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "finance_db"
